# circle.yml
machine:
  node:
    version: 5.11.1

dependencies:
  override:
    - docker build -t hi-ci-workflow

test:
  override:
    - ./node_modules/.bin/mocha
    - docker run -d -p 3000:3000 hi-ci-workflow; sleep 10
    - curl --retry 10 --retry-delay 5 -v http://localhost:3000
